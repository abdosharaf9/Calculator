<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="button" parent="Widget.MaterialComponents.ExtendedFloatingActionButton">
        <item name="android:layout_width">72dp</item>
        <item name="android:layout_height">72dp</item>
        <item name="android:layout_margin">6dp</item>
        <item name="android:fontFamily">@font/sen_regular</item>
        <item name="cornerRadius">20dp</item>
        <item name="android:clipToPadding">false</item>
        <item name="android:clipChildren">false</item>
        <item name="backgroundTint">#F9FBFF</item>
        <item name="android:elevation">10dp</item>
        <item name="android:textSize">@dimen/mainTextSize</item>
        <item name="rippleColor">@color/rippleColor</item>
    </style>

    <style name="digitButton" parent="button">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="operatorButton" parent="button">
        <item name="android:textColor">@color/mainTextColor</item>
    </style>

</resources>